using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AlchemyHelper : MonoBehaviour
{
    public enum IngredientType
    {
        None,
        BlueScale,
        RedScale,
        YellowScale,
        BrownScale,
        GoldenScale,
        PinkScale,
        SilverScale,
    }

    public enum IngredientClass
    {
        AnimalProduct,
    }

    public enum LabType
    {
        ScalesLab,
    }
}
