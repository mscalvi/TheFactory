<UserControl x:Class="DeltaBingoCreator.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeltaBingoCreator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="300"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button x:Name="CreateBtn" Grid.Column="0" Grid.Row="1" Margin="25,25,25,25" Content="Criar Jogo" FontSize="24" Click="CreateViewOpen"/>
        <Button x:Name="EditBtn" Grid.Column="1" Grid.Row="1" Margin="25,25,25,25" Content="Editar Itens" FontSize="24"/>
        <Button x:Name="PrintBtn" Grid.Column="2" Grid.Row="1" Margin="25,25,25,25" Content="Gerar Cartelas" FontSize="24"/>
    </Grid>
</UserControl>
